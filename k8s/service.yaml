apiVersion: v1
kind: Service
metadata:
  name: otel-app-service
  namespace: otel-app
spec:
  selector:
    app: otel-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30300
